export default (ticks: symbolTicks) => {
	// convert this incoming data possibly we will receive this data every second may more than once in every second
	// so we have to convert this incoming ticks data to 1M, 5M and 15M
}
