export const fyersPingHandler = async (ping: any) => {
    //fyers socket sends ping every 5 seconds
    // console.log("ping", ping)
    return
}
export const trueDataPingHandler = async (ping: any) => {
    //trueData socket sends ping every 5 seconds
    // console.log("ping", ping)w
    return
}